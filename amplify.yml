version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm i strapi@alpha -g
        - npm install
    build:
      commands:
        - npm run build
    postBuild:
      commands:
        - npm start:amplify
  artifacts:
    baseDirectory: build
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
