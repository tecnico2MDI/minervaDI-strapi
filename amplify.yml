version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm install -g pm2
        - mkdir -p build
        - npm i strapi@alpha -g
        - npm install
    build:
      commands:
        - npm run build
    postBuild:
      commands:
        - node server.js
  artifacts:
    baseDirectory: ./minervaDI/build
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
